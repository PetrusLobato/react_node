name: js-server

services:
  pg:
    image: bitnami/postgresql
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=b12p23p07
      - POSTGRES_DB=Project_test